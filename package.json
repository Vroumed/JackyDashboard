{
    "scripts": {
        "precommit": "cd client && npm run lint && npm run typecheck",
        "prepare": "husky install"
    },
    "devDependencies": {
        "@commitlint/cli": "^19.4.1",
        "@commitlint/config-conventional": "^19.4.1",
        "husky": "^9.1.6"
    }
}
